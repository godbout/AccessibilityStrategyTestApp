@testable import AccessibilityStrategy
import XCTest
import Common


// this move uses FT innerBlock that is already heavily tested on its own. we still need to test all the things
// specific to dInnerBlock, like the difference between brackets on a single line or several, LYS,
// copied text etc. this is done in UIT.
class ASUT_NM_dInnerBlock_Tests: ASUT_NM_BaseTests {}
