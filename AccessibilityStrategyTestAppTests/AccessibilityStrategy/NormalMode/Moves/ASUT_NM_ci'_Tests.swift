@testable import AccessibilityStrategy
import XCTest


// same mindset as with ci( and co.
// calling cInnerQuotedString, so all the tests are there.
// some UI Tests for each ci' and co, to check that we pass the pgR param correctly to cInnerQuotedString
class ASNM_ciSingleQuote_Tests: ASUT_NM_BaseTests {}
