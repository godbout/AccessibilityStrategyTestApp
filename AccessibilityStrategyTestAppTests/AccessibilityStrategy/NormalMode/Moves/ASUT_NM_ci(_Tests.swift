@testable import AccessibilityStrategy
import XCTest


// calling cInnerBlock, all the tests are there.
// there's one UI Tests for ci(, to check that we pass the pgR correctly to cInnerBlock.
class ASUT_NM_ciLeftParenthesis_Tests: ASUT_NM_BaseTests {}
