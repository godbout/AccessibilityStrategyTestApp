@testable import AccessibilityStrategy
import XCTest


// calling cABlock, all the tests are there.
// there's one UI Tests for ca(, to check that we pass the pgR correctly to cABlock.
class ASUT_NM_caLeftParenthesis_Tests: ASUT_NM_BaseTests {}
