@testable import AccessibilityStrategy
import XCTest


// all tests moved to UI Tests due to PGR that requires a new implementation
// where we use the AX within the move itself to bypass possible restrictions. hence UI Tests.
class ASUT_NM_C__Tests: ASUT_NM_BaseTests {}
