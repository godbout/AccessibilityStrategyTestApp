import XCTest
import AccessibilityStrategy


// VM , (and ;) don't use the state themselves and don't care
// if it's VMC or VML. they just forward the state to the lastLeftRightSearch kinda moves
// so no tests to have here. all the tests are done in F, f, T, t.
class ASUT_VML_comma_Tests: ASUT_VM_BaseTests {}
