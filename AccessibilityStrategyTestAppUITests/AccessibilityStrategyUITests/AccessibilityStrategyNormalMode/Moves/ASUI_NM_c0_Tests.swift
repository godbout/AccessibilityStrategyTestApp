@testable import AccessibilityStrategy
import XCTest
import Common


// this can't be tested automatically.
// because once the start of the currentFileLine is reached with the AX,
// there's no way to test an extra delete through tap :D
class ASUI_NM_cZero_Tests: ASUI_NM_BaseTests {}
